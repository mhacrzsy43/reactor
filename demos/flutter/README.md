# Flutter Reactor demo

This directory contains the framework source. If Android/iOS runner folders are absent, Reactor's Flutter build adapter runs `flutter create --platforms android,ios --org com.reactor .` before building, then applies the fixed bundle IDs from `reactor.config.json`.
